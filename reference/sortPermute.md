# Sort a character vector and return the permutation index

Internal helper used to keep labels aligned after alphabetical
reordering.

## Usage

``` r
sortPermute(stringlist)
```

## Arguments

- stringlist:

  Character vector to sort.

## Value

A list with the sorted vector (`sortlist`) and its permutation index
(`perm`).
